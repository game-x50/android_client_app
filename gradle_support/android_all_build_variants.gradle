android {
    buildTypes {

        debug {
            matchingFallbacks = [BuildTypes.single]
        }

        release {
            matchingFallbacks = [BuildTypes.single]
        }

        staging {
            matchingFallbacks = [BuildTypes.single]
        }
    }
}