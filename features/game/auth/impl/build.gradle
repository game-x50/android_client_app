apply from: rootProject.ext[GradleExtraArgs.baseAndroidLibrary]
apply from: rootProject.ext[GradleExtraArgs.androidSingleBuildVariant]

apply from: rootProject.ext[GradleExtraArgs.dagger2Kapt]
apply from: rootProject.ext[GradleExtraArgs.kotlinxSerilization]

def proguardRulesFile = "proguard-rules-game_auth_impl.pro"

android {
    defaultConfig {
        versionCode = 1
        versionName = "1.0"

        consumerProguardFiles(proguardRulesFile)
    }

    resourcePrefix("game_auth_impl_")
}

dependencies {
    api(projects.features.thirdParty.rxjava2.extensions)

    compileOnly(projects.features.core.api.lib)
    compileOnly(projects.features.core.android.api)
    compileOnly(projects.features.core.extensions)
    compileOnly(projects.features.game.api.lib)
    compileOnly(projects.features.core.network.api)
    compileOnly(projects.features.core.android.storage)
    compileOnly(projects.features.core.error)

    compileOnly(Deps.threeTenBp)

    implementation platform(Deps.firebaseBom)
    implementation(Deps.firebaseAuth)
    implementation(Deps.firebaseFirestore)
}