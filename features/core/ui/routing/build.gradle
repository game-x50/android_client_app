apply from: rootProject.ext[GradleExtraArgs.baseAndroidLibrary]
apply from: rootProject.ext[GradleExtraArgs.androidSingleBuildVariant]

apply from: rootProject.ext[GradleExtraArgs.kapt]
apply plugin: Plugins.kotlinParcelize

android {

    defaultConfig {
        versionCode = 1
        versionName = "1.0"
    }

//    resourcePrefix("core_ui_api_")
}

dependencies {
    compileOnly(projects.features.core.api.lib)
    compileOnly(projects.features.core.ui.api.lib)

    api(Deps.cicerone)

    compileOnly(Deps.androidxFragment)
}