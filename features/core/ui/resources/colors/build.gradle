apply from: GradleExtraArgs.getBaseAndroidResourcesLibrary(rootProject)
apply from: GradleExtraArgs.getAndroidSingleBuildVariant(rootProject)

android {

    defaultConfig {
        versionCode = 1
        versionName = "1.0"
    }

//    resourcePrefix("core_ui_api_")
}