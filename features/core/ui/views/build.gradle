apply from: rootProject.ext[GradleExtraArgs.baseAndroidLibrary]
apply from: rootProject.ext[GradleExtraArgs.androidSingleBuildVariant]

android {

    defaultConfig {
        versionCode = 1
        versionName = "1.0"
    }

//    resourcePrefix("core_ui_api_")
}

dependencies {
    compileOnly(projects.features.core.android.extensions)
    compileOnly(projects.features.core.ui.api.lib)

    compileOnly(Deps.androidMaterial)// todo: implementation instead of compileOnly because of task ./gradlew assembleStaging failing on CI, try to use compileOnly
    compileOnly(Deps.constraintLayout)// todo: implementation instead of compileOnly because of task ./gradlew assembleStaging failing on CI, try to use compileOnly
}