apply from: rootProject.ext[GradleExtraArgs.baseAndroidLibrary]
apply from: rootProject.ext[GradleExtraArgs.androidSingleBuildVariant]

android {
    defaultConfig {
        versionCode = 1
        versionName = "1.0"
    }

//    resourcePrefix("core_ui_api_test_utils")
}

dependencies {
    api(projects.features.core.testUtils)
    api(projects.features.core.ui.viewmodel.lib)

    compileOnly(projects.features.core.extensions)
}