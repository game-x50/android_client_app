apply from: rootProject.ext[GradleExtraArgs.baseAndroidLibrary]
apply from: rootProject.ext[GradleExtraArgs.androidSingleBuildVariant]

apply from: rootProject.ext[GradleExtraArgs.dagger2Kapt]
apply from: rootProject.ext[GradleExtraArgs.viewBinding]

android {

    defaultConfig {
        versionCode = 1
        versionName = "1.0"
    }

    resourcePrefix("core_ui_impl_staging_tools_")
}

dependencies {
    compileOnly(projects.features.core.extensions)
    compileOnly(projects.features.core.android.extensions)
    compileOnly(projects.features.thirdParty.androidx.insets)
    compileOnly(projects.features.core.api.lib)
    compileOnly(projects.features.core.android.api)
    implementation(projects.features.core.ui.api.lib)// todo: implementation instead of compileOnly because of task ./gradlew assembleStaging failing, try to use compileOnly

    compileOnly(projects.features.core.api.stagingTools)

    compileOnly(Deps.rxJava2)

    compileOnly(Deps.androidxAppCompat)
    implementation(Deps.androidMaterial)// todo: implementation instead of compileOnly because of task ./gradlew assembleStaging failing on CI, try to use compileOnly
    implementation(Deps.constraintLayout)// todo: implementation instead of compileOnly because of task ./gradlew assembleStaging failing on CI, try to use compileOnly
    compileOnly(Deps.swipeRefreshLayout)

    compileOnly(Deps.cicerone)
}