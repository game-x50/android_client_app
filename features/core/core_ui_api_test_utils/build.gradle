apply from: rootProject.ext[GradleExtraArgs.baseAndroidLibrary]
apply from: rootProject.ext[GradleExtraArgs.androidSingleBuildVariant]

def androidCoreUiApiTestUtilsLibraryVersionName = "1"
def androidCoreUiApiTestUtilsLibraryVersionCode = 1

android {
    defaultConfig {
        versionCode = androidCoreUiApiTestUtilsLibraryVersionCode
        versionName = androidCoreUiApiTestUtilsLibraryVersionName
    }

    resourcePrefix("core_ui_api_test_utils")
}

dependencies {
    compileOnly(project(LocalGradleProjects.testUtils))
    compileOnly(project(LocalGradleProjects.coreUiApi))

    implementation(Deps.lifecycleExtensions)
}