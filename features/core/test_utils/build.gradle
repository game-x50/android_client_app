apply from: GradleExtraArgs.getBaseKotlinLibrary(rootProject)

dependencies {
    api(Deps.jUnit)

    implementation(Deps.threeTenBpZonedTest) {
        exclude group: "com.jakewharton.threetenabp", module: "threetenabp"
    }
}