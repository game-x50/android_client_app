apply from: rootProject.ext[GradleExtraArgs.baseAndroidLibrary]
apply from: rootProject.ext[GradleExtraArgs.androidSingleBuildVariant]

apply from: rootProject.ext[GradleExtraArgs.dagger2Kapt]

def coreImplDebugToolsLibraryVersionName = "1"
def coreImplDebugToolsLibraryVersionCode = 1

android {

    defaultConfig {
        versionCode = coreImplDebugToolsLibraryVersionCode
        versionName = coreImplDebugToolsLibraryVersionName
    }

    resourcePrefix("core_impl_debug_tools_")
}

dependencies {
    compileOnly(project(LocalGradleProjects.extensions))
    compileOnly(project(LocalGradleProjects.androidCoreApi))
    compileOnly(project(LocalGradleProjects.coreApi))

    compileOnly(project(LocalGradleProjects.coreApiDebugTools))
    api(project(LocalGradleProjects.coreImplStagingTools))

    implementation(Deps.stetho)
}